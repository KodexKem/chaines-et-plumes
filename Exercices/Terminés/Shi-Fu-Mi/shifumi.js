function PIERRE() {
  let aleatoire = Math.floor(Math.random() * 3);
  let Joueur = 0;
  if (Joueur === 0 && aleatoire === 0) {
    document.querySelector("#affiche").textContent = "EGALITÉ!";
  } else if (Joueur === 0 && aleatoire === 1) {
    document.querySelector("#affiche").innerHTML =
      '<img src="GorillaFuck.jpg"/>';
  } else {
    Joueur === 0 && aleatoire === 2;
    document.querySelector("#affiche").textContent = "GAGNÉ!";
  }
}
function FEUILLE() {
  let aleatoire = Math.floor(Math.random() * 3);
  let Joueur = 0;
  if (Joueur === 0 && aleatoire === 0) {
    document.querySelector("#affiche").textContent = "EGALITÉ!";
  } else if (Joueur === 0 && aleatoire === 1) {
    document.querySelector("#affiche").innerHTML =
      '<img src="GorillaFuck.jpg"/>';
  } else {
    Joueur === 0 && aleatoire === 2;
    document.querySelector("#affiche").textContent = "GAGNÉ!";
  }
}
function CISEAUX() {
  let aleatoire = Math.floor(Math.random() * 3);
  let Joueur = 0;
  if (Joueur === 0 && aleatoire === 0) {
    document.querySelector("#affiche").textContent = "EGALITÉ!";
  } else if (Joueur === 0 && aleatoire === 1) {
    document.querySelector("#affiche").innerHTML =
      '<img src="GorillaFuck.jpg"/>';
  } else {
    Joueur === 0 && aleatoire === 2;
    document.querySelector("#affiche").textContent = "GAGNÉ!";
  }
}
